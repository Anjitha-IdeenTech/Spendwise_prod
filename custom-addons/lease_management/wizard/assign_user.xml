

<odoo>
    <data>
        <record id="view_assign_user_wizard_form" model="ir.ui.view">
            <field name="name">assign.user.form</field>
            <field name="model">assign.user</field>
            <field name="arch" type="xml">
                <form string="Select User">
                    <group>

                        <field name="user_id" widget="many2many_tags" options="{'no_edit': True, 'no_create': True ,'no_open': True,'no_quick_create':True}"/>

                    </group>
                    <footer>

                        <button name="action_confirm" string="Assign" type="object" class="btn-primary " style="float: right;"
                                confirm="Are you sure you want to assign this Purchase Order to this user?"/>
                        <div style="float: right;padding-right: 10px;">
                         <button string="Cancel" class="btn-secondary" special="cancel"  />
                        </div>
                    </footer>
                </form>
            </field>

        </record>

<record id="view_assign_user_action" model="ir.actions.act_window">
            <field name="name">Assign</field>
            <field name="res_model">assign.user</field>
            <field name="view_mode">form</field>
            <field name="view_id" ref="view_assign_user_wizard_form"/>
            <field name="target">new</field>
        </record>
    </data>
</odoo>
